[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
# tokio.workspace = true

api = { path = "../api" }
core = { path = "../core" }
index = { path = "../index" }
# storage = { path = "../storage" }