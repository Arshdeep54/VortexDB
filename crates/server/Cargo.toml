[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
# tokio.workspace = true

core = { path = "../core" }
index = { path = "../index" }
# storage = { path = "../storage" }
# api = { path = "../api" }